services:
  - type: web
    name: vocab-backend
    runtime: node
    plan: free
    buildCommand: echo "no build step"
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: GH_TOKEN
        sync: false
      - key: GH_REPO
        value: SctrlB/vocab-backend
      - key: STATE_PATH
        value: data/state.json
